GRANT SELECT on `examsearch`.* to PHP@localhost
GRANT INSERT on `examsearch`.`clicks` to PHP@localhost
GRANT INSERT on `examsearch`.`uses` to PHP@localhost
GRANT UPDATE on `examsearch`.`uses` to PHP@localhost
